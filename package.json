{
  "name": "pennsylvania-women-work",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "api",
    "app"
  ],
  "scripts": {
    "start:dev:api": "cd api && npm start",
    "start:dev:app": "cd app && npm start",
    "start:prod:api": "cd api && npm run start:prod",
    "start:prod:app": "cd app && npm run start:prod",
    "start:dev": "concurrently \"npm run start:dev:api\" \"sleep 15 && npm run start:dev:app\"",
    "start:prod": "concurrently \"npm run start:prod:api\" \"echo \\\"==App starts in 15==\\\" && sleep 15 && npm run start:prod:app\"",
    "build": "npm run build:api && npm run build:app",
    "build:api": "cd api && npm run build",
    "build:app": "cd app && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
